# Interesting Files

* **Log Files**
  * `/var/log/sshd.log`
  * `/var/log/mail`
  * `/var/log/vsftpd.log`
* **Process Files**: If log access is restricted, you can try including process files like:
  * `/proc/self/environ`
  * `/proc/self/fd/N`
